<div >
    <% if current_user.friends.all.where(id: user.id ).empty? == false %>
        <p>nothing</p>
    <% else %>
        <%= button_to 'Add Friend?', friend_requests_path(id: user.id), method: :post %>
    <% end %>
</div>